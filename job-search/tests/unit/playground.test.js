import { describe, it, expect } from 'vitest'

describe('describe = Describes what we are testing', () => {
  it('add two numbers', () => {
    // expect = assertion
    // toBe = expected result
    expect(1 + 1).toBe(2)
  })
})
